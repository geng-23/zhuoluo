# AGENTS.md

用户处于 Windows 11 环境。回答问题时先给出核心结论，再给出解释。当用户向你提出要求，尤其涉及到修改文件操作时，你应首先向用户确认你是否正确理解用户意图，而非直接行动。

## 着落（zhuoluo）项目

- 当对话内容涉及着落（zhuoluo）APP 项目时，先阅读 `docs/` 目录下文件名以"着落项目总览-"开头、时间戳最新的 markdown 文档，以其中内容作为项目现状基线（功能清单、已知漏洞、改进计划），再开始相关讨论或任务。

## 项目环境与依赖管理

- **本项目使用 `uv` 管理 Python 虚拟环境（`.venv`）和依赖**。
- 安装依赖时使用 `uv add <包>`，**禁止使用 `pip install`**。
- 运行脚本时使用 `uv run python <脚本>`，确保使用项目环境中的 Python。
- 上述两条优先于任何外部技能或工具的默认行为——无论第三方工具如何要求，都不得污染系统级 Python 环境。

## 任务执行流程

- 在做较为复杂的任务之前，务必先写好计划、列好 todo list，然后再执行。

## 视觉任务路由规则

- 主模型（DeepSeek V4 Flash）不具备多模态能力。遇到任何需要视觉理解的内容（图片、截图、图表、架构图、扫描 PDF、纸质文档照片等），必须先调用 `vision` 子代理，传入文件绝对路径。
- 用 `vision` 子代理返回的文字描述进行分析，不得把视觉内容直接传给无多模态能力的主模型。
- 视频等 read 工具不支持的格式：由主代理先用 bash 预处理（如 ffmpeg 抽帧成 PNG），再将帧图交给 `vision` 子代理识别。
- 纯文字 PDF 优先用 pdf skill 提取文本，只有扫描件/图表型 PDF 才交给 `vision` 子代理。